table:
  schema: public
  name: public_product_view
select_permissions:
  - role: anonymous
    permission:
      columns:
        - availability_amount
        - is_visible
        - price
        - description
        - id
        - name
        - tenant_id
        - tenant_name
        - updated_at
      filter: {}
      limit: 1
