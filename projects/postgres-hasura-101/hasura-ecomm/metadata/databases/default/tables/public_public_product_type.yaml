table:
  schema: public
  name: public_product_type
select_permissions:
  - role: anonymous
    permission:
      columns:
        - availability
        - is_visible
        - price
        - description
        - id
        - name
        - tenant_id
        - tenant_name
        - updated_at
      filter: {}
      limit: 1
