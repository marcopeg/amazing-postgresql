table:
  schema: public
  name: public_products_cached
select_permissions:
  - role: anonymous
    permission:
      columns:
        - availability_amount
        - availability_updated_at
        - description
        - id
        - name
        - price
        - tenant_id
        - tenant_name
        - updated_at
      filter: {}
      limit: 25
